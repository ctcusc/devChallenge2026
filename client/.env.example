# Copy this file to .env (or .env.local) for the Next.js client.

# Base URL of the Feeding Brennen API server.
# Must be prefixed with NEXT_PUBLIC_ to be readable from client components.
NEXT_PUBLIC_API_URL=http://localhost:3001
